# ==========================================================================
# Railway / Nixpacks configuration
# บอกให้ใช้ PHP + Apache และ document root คือโฟลเดอร์หลักของโปรเจกต์
# ==========================================================================
[phases.setup]
nixPkgs = ["php82", "php82Packages.composer"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
